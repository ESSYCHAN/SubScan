// import { describe, it, expect } from 'vitest';
// import { confidenceTone } from '../../components/StatementReview';

// describe('confidenceTone', () => {
//   it('returns low for <50', () => {
//     expect(confidenceTone(0)).toBe('low');
//     expect(confidenceTone(49)).toBe('low');
//   });
//   it('returns med for 50–74', () => {
//     expect(confidenceTone(50)).toBe('med');
//     expect(confidenceTone(74)).toBe('med');
//   });
//   it('returns high for ≥75', () => {
//     expect(confidenceTone(75)).toBe('high');
//     expect(confidenceTone(100)).toBe('high');
//   });
// });
